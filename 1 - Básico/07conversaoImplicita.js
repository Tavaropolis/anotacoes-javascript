// Quando ocorre a soma de uma string e um number (concatenação)
let x = 16 + "Volvo";
console.log(x);

//A atribuição ocorre sempre da esquerda para a direita
    // Soma antes e depois concatenação
    let y = 16 + 4 + "Volvo";
    console.log(y);
    // Concatenação sem soma;
    let z = "Volvo" + 16 + 4;
    console.log(z);